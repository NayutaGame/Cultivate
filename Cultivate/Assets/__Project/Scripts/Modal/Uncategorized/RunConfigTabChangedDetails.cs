
public class RunConfigTabChangedDetails
{
    public int FromIndex;
    public int ToIndex;

    public RunConfigTabChangedDetails(int fromIndex, int toIndex)
    {
        FromIndex = fromIndex;
        ToIndex = toIndex;
    }
}