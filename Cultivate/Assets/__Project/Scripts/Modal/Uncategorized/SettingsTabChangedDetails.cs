
public class SettingsTabChangedDetails
{
    public int FromIndex;
    public int ToIndex;

    public SettingsTabChangedDetails(int fromIndex, int toIndex)
    {
        FromIndex = fromIndex;
        ToIndex = toIndex;
    }
}